apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.transaction.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.services.transaction.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.services.transaction.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.transaction.name }}
    spec:
      containers:
        - name: {{ .Values.services.transaction.name }}
          image: "{{ .Values.services.transaction.image.repository }}:{{ .Values.services.transaction.image.tag }}"
          imagePullPolicy: {{ .Values.services.transaction.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.transaction.port }}
              name: http
          env:
            - name: DATASOURCE_URL
              value: jdbc:postgresql://{{ .Values.postgres.name }}:{{ .Values.postgres.port }}/{{ .Values.services.transaction.database }}
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: launchpad-secrets
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: launchpad-secrets
                  key: POSTGRES_PASSWORD
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: launchpad-config
                  key: REDIS_HOST
            - name: RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: launchpad-config
                  key: RABBITMQ_HOST
            - name: SPRING_RABBITMQ_USERNAME
              valueFrom:
                secretKeyRef:
                  name: launchpad-secrets
                  key: RABBITMQ_USERNAME
            - name: SPRING_RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: launchpad-secrets
                  key: RABBITMQ_PASSWORD
          startupProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.transaction.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.services.transaction.name }}
  ports:
    - name: http
      port: {{ .Values.services.transaction.port }}
      targetPort: {{ .Values.services.transaction.port }}