apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.campaign.name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.services.campaign.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.services.campaign.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.campaign.name }}
    spec:
      containers:
        - name: {{ .Values.services.campaign.name }}
          image: "{{ .Values.services.campaign.image.repository }}:{{ .Values.services.campaign.image.tag }}"
          imagePullPolicy: {{ .Values.services.campaign.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.campaign.port }}
              name: http
          env:
            - name: DATASOURCE_URL
              value: jdbc:postgresql://{{ .Values.postgres.name }}:{{ .Values.postgres.port }}/{{ .Values.services.campaign.database }}
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: launchpad-secrets
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: launchpad-secrets
                  key: POSTGRES_PASSWORD
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: launchpad-config
                  key: REDIS_HOST
            - name: RABBITMQ_HOST
              valueFrom:
                configMapKeyRef:
                  name: launchpad-config
                  key: RABBITMQ_HOST
            - name: SPRING_RABBITMQ_USERNAME
              valueFrom:
                secretKeyRef:
                  name: launchpad-secrets
                  key: RABBITMQ_USERNAME
            - name: SPRING_RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: launchpad-secrets
                  key: RABBITMQ_PASSWORD
          startupProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.campaign.name }}
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.services.campaign.name }}
  ports:
    - name: http
      port: {{ .Values.services.campaign.port }}
      targetPort: {{ .Values.services.campaign.port }}